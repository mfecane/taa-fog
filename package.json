{
  "name": "taa-fog",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:build": "npm run build",
    "deploy:check": "npm run build && echo 'Build completed successfully. Ready for deployment.'",
    "deploy:preview": "npm run build && npm run preview"
  },
  "devDependencies": {
    "@types/dat.gui": "^0.7.13",
    "@types/three": "^0.181.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.2",
    "vite-plugin-glsl": "^1.5.4"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "three": "^0.181.0"
  }
}
